name: "Update Korean TV M3U Playlist"

on:
  schedule:
    - cron: '0 0 * * *'  # 每天UTC时间00:00运行
  workflow_dispatch:
  push:
    paths:
      - 'update_playlist.py'

permissions:
  contents: write

jobs:
  update-playlist:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.FULL_ACCESS_TOKEN }}
        # 移除 fetch-depth: 0，使用默认设置

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests selenium beautifulsoup4 webdriver-manager

    - name: Run update script
      env:
        FULL_ACCESS_TOKEN: ${{ secrets.FULL_ACCESS_TOKEN }}
      run: |
        python update_playlist.py

    - name: Configure Git
      run: |
        git config --global user.email "action@github.com"
        git config --global user.name "GitHub Action"

    - name: Smart commit without conflicts
      run: |
        echo "🔄 开始智能提交流程..."
        
        # 首先拉取最新的远程更改
        echo "📥 拉取最新远程更改..."
        git pull origin main
        
        # 重新生成播放列表（基于最新代码）
        echo "🔄 重新生成播放列表..."
        python update_playlist.py
        
        # 检查是否有实际内容变化（忽略时间戳）
        echo "🔍 检查内容变化..."
        if git diff --ignore-all-space --quiet korean_tv.m3u korean_tv_china_optimized.m3u; then
          echo "📝 播放列表内容无实质变化，跳过提交"
          exit 0
        fi
        
        # 提交更改
        echo "💾 提交播放列表更新..."
        git add korean_tv.m3u korean_tv_china_optimized.m3u
        git commit -m "Auto-update playlist - $(date +'%Y-%m-%d %H:%M:%S')"
        
        # 推送更改
        echo "🚀 推送更新..."
        git push origin main
        
        echo "✅ 播放列表更新完成！"

    - name: Upload playlist as artifact
      uses: actions/upload-artifact@v4
      with:
        name: korean-tv-playlists
        path: |
          korean_tv.m3u
          korean_tv_china_optimized.m3u
        retention-days: 7
