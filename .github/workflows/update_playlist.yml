name: "Update M3U Playlist"

on:
  schedule:
    - cron: '0 0 */2 * *'  # 每2天UTC时间0点运行一次
  workflow_dispatch:      # 支持手动触发

permissions:
  contents: write

jobs:
  update-playlist:
    runs-on: ubuntu-latest
    
    steps:
    # 1. 检出代码
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.FULL_ACCESS_TOKEN }}

    # 2. 设置Python环境
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    # 3. 安装依赖
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests selenium beautifulsoup4

    # 4. 安装Chrome浏览器
    - name: Install Chrome
      uses: browser-actions/setup-chrome@v1

    # 5. 运行更新脚本
    - name: Run update script
      env:
        FULL_ACCESS_TOKEN: ${{ secrets.FULL_ACCESS_TOKEN }}
      run: |
        python update_playlist.py

    # 6. 提交并推送到GitHub
    - name: Commit and push changes
      run: |
        git config --global user.email "action@github.com"
        git config --global user.name "GitHub Action"
        git add -A
        git commit -m "Auto-update playlist - $(date +'%Y-%m-%d %H:%M:%S')" || exit 0
        git push origin main

    # 7. 输出最终状态
    - name: Output status
      run: |
        echo "✅ 自动化更新完成!"
        echo "🔗 GitHub播放列表链接: https://raw.githubusercontent.com/GoonhoLee/korean-tv-static/main/korean_tv.m3u"
        echo "⏰ 下次自动运行: 2天后 UTC 0:00"
